# Copy this file to secrets.toml and replace only the placeholder value.
# Never commit secrets.toml or paste a real key into this example file.
OPENAI_API_KEY = "replace-with-your-key"
SUPABASE_URL = "https://your-project-ref.supabase.co"
SUPABASE_PUBLISHABLE_KEY = "replace-with-your-supabase-publishable-key"
